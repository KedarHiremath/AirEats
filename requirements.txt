# Core framework
Django==5.1.2             # latest stable (works with Python 3.13)

# MongoDB ODM for Django
mongoengine==0.29.1       # official MongoEngine (stable release)

# REST framework for APIs
djangorestframework==3.15.2
django-rest-framework-mongoengine==3.4.1

# Utils
dnspython==2.6.1          # required for MongoDB Atlas connection strings
python-decouple==3.8      # manage secrets (DB URI, secret key, etc.) via .env
pyjwt==2.9.0              # for login/auth (JWT tokens)

# (Optional but recommended for dev)
pytz==2024.2              # timezone support